# Server-only — never prefix with NEXT_PUBLIC_
# Shared secret for HIQ public leads ingest (X-API-Key header).
HIQ_LEADS_API_KEY=

# HIQ API origin with no trailing slash (e.g. https://api.example.com)
HIQ_BASE_URL=
